cmake_minimum_required(VERSION 3.15)
project(compression_consumer LANGUAGES C)
find_package(aws-c-compression CONFIG REQUIRED)
add_executable(compression_consumer consumer.c)
target_link_libraries(compression_consumer PRIVATE AWS::aws-c-compression)
